import { CentToEuroPipe } from './cent-to-euro.pipe';

describe('CentToEuroPipe', () => {
  it('create an instance', () => {
    const pipe = new CentToEuroPipe();
    expect(pipe).toBeTruthy();
  });
});
